# Rust callers — `core-foundation-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `core-foundation` | `core_foundation` | 12 | 3 | 2 | 5 |

## `core-foundation`

### Inbound symbols (9 distinct, 12 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `core-foundation`.

#### `core_foundation::base::TCFType` — 3 site(s) (2 codex, 1 other, 3 caller crates)

- ✅ `codex-kotlin/codex-config` `src/loader/macos.rs:7` — `use core_foundation::base::TCFType`
- ✅ `codex-kotlin/codex-utils-sleep-inhibitor` `src/macos.rs:1` — `use core_foundation::base::TCFType`
- `webbrowser-kotlin/webbrowser` `src/macos.rs:3` — `use core_foundation::base::TCFType`

#### `core_foundation::string::CFString` — 2 site(s) (2 codex, 0 other, 2 caller crates)

- ✅ `codex-kotlin/codex-config` `src/loader/macos.rs:8` — `use core_foundation::string::CFString`
- ✅ `codex-kotlin/codex-utils-sleep-inhibitor` `src/macos.rs:2` — `use core_foundation::string::CFString`

#### `core_foundation::string::CFStringRef` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-config` `src/loader/macos.rs:9` — `use core_foundation::string::CFStringRef`

#### `core_foundation::array::CFArray` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `webbrowser-kotlin/webbrowser` `src/macos.rs:2` — `use core_foundation::array::CFArray`

#### `core_foundation::array::CFArrayRef` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `webbrowser-kotlin/webbrowser` `src/macos.rs:2` — `use core_foundation::array::CFArrayRef`

#### `core_foundation::error::CFError` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `webbrowser-kotlin/webbrowser` `src/macos.rs:4` — `use core_foundation::error::CFError`

#### `core_foundation::error::CFErrorRef` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `webbrowser-kotlin/webbrowser` `src/macos.rs:4` — `use core_foundation::error::CFErrorRef`

#### `core_foundation::url::CFURL` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `webbrowser-kotlin/webbrowser` `src/macos.rs:5` — `use core_foundation::url::CFURL`

#### `core_foundation::url::CFURLRef` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `webbrowser-kotlin/webbrowser` `src/macos.rs:5` — `use core_foundation::url::CFURLRef`
