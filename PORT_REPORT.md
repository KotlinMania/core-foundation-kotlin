# Port report — `core-foundation-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `core-foundation`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 12 `use` sites from 2 other repos
- **Outbound**: 5 `use` sites to 2 other repos
- **Unresolved external**: 1 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `core-foundation` | `core_foundation` | 12 | 2 | COD-34 | 129 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (12 edges, 3 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `webbrowser-kotlin` | `webbrowser` | 7 |  |
| `codex-kotlin` | `codex-config` | 3 | ✅ |
| `codex-kotlin` | `codex-utils-sleep-inhibitor` | 2 | ✅ |

### Symbol-level demand (9 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `core_foundation::base::TCFType` | 3 | 2 | 1 | 3 |
| `core_foundation::string::CFString` | 2 | 2 | 0 | 2 |
| `core_foundation::string::CFStringRef` | 1 | 1 | 0 | 1 |
| `core_foundation::array::CFArray` | 1 | 0 | 1 | 1 |
| `core_foundation::array::CFArrayRef` | 1 | 0 | 1 | 1 |
| `core_foundation::error::CFError` | 1 | 0 | 1 | 1 |
| `core_foundation::error::CFErrorRef` | 1 | 0 | 1 | 1 |
| `core_foundation::url::CFURL` | 1 | 0 | 1 | 1 |
| `core_foundation::url::CFURLRef` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 9 symbols)

##### `core_foundation::base::TCFType` (3 sites, 2 codex, 1 other, 3 caller crates)

Codex call sites:

- `codex-kotlin/codex-config` `src/loader/macos.rs:7` — `use core_foundation::base::TCFType`
- `codex-kotlin/codex-utils-sleep-inhibitor` `src/macos.rs:1` — `use core_foundation::base::TCFType`

Other workspace call sites:

- `webbrowser-kotlin/webbrowser` `src/macos.rs:3` — `use core_foundation::base::TCFType`

##### `core_foundation::string::CFString` (2 sites, 2 codex, 0 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-config` `src/loader/macos.rs:8` — `use core_foundation::string::CFString`
- `codex-kotlin/codex-utils-sleep-inhibitor` `src/macos.rs:2` — `use core_foundation::string::CFString`

##### `core_foundation::string::CFStringRef` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-config` `src/loader/macos.rs:9` — `use core_foundation::string::CFStringRef`

##### `core_foundation::array::CFArray` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `webbrowser-kotlin/webbrowser` `src/macos.rs:2` — `use core_foundation::array::CFArray`

##### `core_foundation::array::CFArrayRef` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `webbrowser-kotlin/webbrowser` `src/macos.rs:2` — `use core_foundation::array::CFArrayRef`

##### `core_foundation::error::CFError` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `webbrowser-kotlin/webbrowser` `src/macos.rs:4` — `use core_foundation::error::CFError`

##### `core_foundation::error::CFErrorRef` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `webbrowser-kotlin/webbrowser` `src/macos.rs:4` — `use core_foundation::error::CFErrorRef`

##### `core_foundation::url::CFURL` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `webbrowser-kotlin/webbrowser` `src/macos.rs:5` — `use core_foundation::url::CFURL`

##### `core_foundation::url::CFURLRef` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `webbrowser-kotlin/webbrowser` `src/macos.rs:5` — `use core_foundation::url::CFURLRef`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `libc-kotlin` | 3 | 3 | `libc` | COD-75 | 3 | To Do |
| `uuid-kotlin` | 2 | 1 | `uuid` | COD-181 | 21 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `core-foundation-sys` | 84 | 49 | `core-foundation` |
