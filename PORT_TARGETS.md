# Port targets — `core-foundation-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `core-foundation` that codex-rs uses, and therefore the minimum subset that `core-foundation-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-34] (rank `129`, tier `direct`, status `To Do`)

**Cargo package name**: `core-foundation`
**Rust module name (in `use` statements)**: `core_foundation`

## Summary

- **3** distinct symbols imported by codex-rs
- **5** total `use` statements
- **2** codex source files affected
- **2** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `core_foundation::base::TCFType`

- **2 `use` site(s)** across **2 codex member crate(s)**: `codex-config`, `codex-utils-sleep-inhibitor`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `config/src/loader/macos.rs:7` (`codex-config`) — `use core_foundation::base::TCFType`
- `utils/sleep-inhibitor/src/macos.rs:1` (`codex-utils-sleep-inhibitor`) — `use core_foundation::base::TCFType`

### `core_foundation::string::CFString`

- **2 `use` site(s)** across **2 codex member crate(s)**: `codex-config`, `codex-utils-sleep-inhibitor`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `config/src/loader/macos.rs:8` (`codex-config`) — `use core_foundation::string::CFString`
- `utils/sleep-inhibitor/src/macos.rs:2` (`codex-utils-sleep-inhibitor`) — `use core_foundation::string::CFString`

### `core_foundation::string::CFStringRef`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-config`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `config/src/loader/macos.rs:9` (`codex-config`) — `use core_foundation::string::CFStringRef`
